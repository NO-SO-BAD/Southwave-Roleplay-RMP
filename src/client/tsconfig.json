{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "../../client_packages/southwave-roleplay",
    "rootDir": "./src",  // Apunta SOLO a src/client/src
    "types": ["@ragempcommunity/types-client"]  // FIX: Tipos client-side (mp.players.local, mp.game, etc.)
  },
  "include": ["src/**/*"]  // Solo archivos dentro de src/client/src
}