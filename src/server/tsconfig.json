{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "../../packages/southwave-roleplay",
    "rootDir": "../../",   // ← CLAVE: Apunta a la carpeta src/ (contiene shared, server y client)
    "types": ["@ragempcommunity/types-server"]
  },
  "include": [
    "src/**/*",              // Tus archivos server locales
    "../../shared/**/*"      // Incluye explícitamente shared
  ],
  "exclude": ["../../client/**/*"]  // Opcional: evita incluir client por error
}